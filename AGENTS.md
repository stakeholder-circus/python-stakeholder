# python-stakeholder AGENTS

1. Use `pyproject.toml`.
2. Commands:
   - install/dev as defined in pyproject
   - `pytest` (or stated test command)
3. Use typed domain model (`dataclass`, `Enum`, `Protocol` where useful).
4. Wire `core/` submodule and shared fixtures.
5. Implement deterministic mode and JSON output.
