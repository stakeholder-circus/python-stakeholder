# Contributing

Keep runtime behavior fail-fast until audited Rust traceability exists.

## Working commands
- `ruff format --check src tests`
- `ruff check src tests`
- `python -m build`
- `pytest`
- `docker build -t python-stakeholder .`
- `docker run --rm python-stakeholder --list-values`
